rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /notes/{noteId} {
      allow read, write: if true;
      
      // Allow access to the updates sub-collection for real-time deltas
      match /updates/{updateId} {
        allow read, write: if true;
      }
    }
  }
}
